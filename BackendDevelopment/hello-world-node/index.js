`use strict`;
// you can either write Node code in the js file or in the REPL(Read -Eval-Print Loop).

// console.log(`hello node, Backend Development`);
const arr = [1,2,3,4,5,6];
// console.log(arr,`arr`);
// setTimeout(()=>{
//     console.log("Asyncronous code delayed by 5 seconds");
// },5000);

// console.log(`This is the last line of sync code!`);

// Node Module-System
// Node Module system allows you to organize your large codebase into multiple re-usable modules. Each file in your node project is trated as seprate module.
